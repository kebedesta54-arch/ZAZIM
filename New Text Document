import { NavLink } from 'react-router-dom'

const linkBase =
  'px-3 py-2 rounded-md text-sm font-medium transition-colors'
const linkActive = 'bg-blue-600 text-white'
const linkIdle = 'text-blue-700 hover:bg-blue-100'

export default function NavBar() {
  const item = ({ isActive }) =>
    `${linkBase} ${isActive ? linkActive : linkIdle}`

  return (
    <nav className="w-full border-b bg-white/70 backdrop-blur">
      <div className="max-w-3xl mx-auto px-4 py-3 flex items-center gap-2">
        <span className="text-xl font-semibold text-blue-700 mr-auto">
          Zazim
        </span>

        <NavLink to="/" className={item} end>
          Home
        </NavLink>
        <NavLink to="/diagnostics" className={item}>
          Diagnostics
        </NavLink>
        <NavLink to="/movers" className={item}>
          Movers
        </NavLink>
        <NavLink to="/contact" className={item}>
          Contact
        </NavLink>
      </div>
    </nav>
  )
}
